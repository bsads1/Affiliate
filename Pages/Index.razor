@page "/"
@layout WebLayout

@code {
    [CascadingParameter] public HttpContext Context { get; set; }
    [CascadingParameter] public PageState PageState { get; set; } = default!;

    protected override void OnInitialized()
    {
        //Log.Information("This is the homepage.");
    }

}

<PageTitle>@PageState.Config?.PageName</PageTitle>

<div class="container h-full">
    <div class="mx-auto max-w-2xl py-16 text-center">
        <h1 class="mb-8 font-display text-4xl font-medium text-jacarta-700 dark:text-white">About Xhibiter</h1>
        <p class="text-lg leading-normal dark:text-jacarta-300">
            Every digital creation available through MakersPlace is an authentic and truly unique digital creation,
            signed and issued by the creator — made possible by blockchain technology.
        </p>
    </div>
    <div class="mx-auto max-w-2xl py-16 text-center">
        <h1 class="mb-8 font-display text-4xl font-medium text-jacarta-700 dark:text-white">Live</h1>
        <div hx-get="/TopNewLivestream" hx-swap="outerHTML"></div>
    </div>
</div>